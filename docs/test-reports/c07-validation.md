# 三档确定性模拟数据验证记录

日期：2026-08-16  
基线 SHA：`3ebd588222ba81157682a096cd2c7d1fd3e81c4b`  
数据集版本：`1.0.0`  
生成器版本：`1.0.0`  
固定种子：`20260816`  
固定基准时间：`2026-08-16T00:00:00Z`  
契约版本：`2.2.0`

## 已执行证据

- 三档生成与校验：small 为 31 个文件、268 个主对象 ID；standard 为 62 个文件、2,459 个主对象 ID；stress 为 62 个文件、15,833 个主对象 ID，均通过对象数量、ID、引用、状态、高风险题目、文件 SHA-256 和 manifest 校验；
- 确定性：自动化测试在两个独立临时根目录连续生成 small，逐文件字节和 manifest 哈希完全一致；
- 启动隔离：自动化测试确认 small 快速路径不生成 stress；standard/stress 只在显式命令下生成，普通安装、启动和默认测试不触发；
- 匿名导出：`pnpm data:export -- --profile small --anonymized` 通过。自动化测试确认导出采用字段白名单、用户名称稳定替换，且不含密码、凭据、Token、Cookie、会话、环境变量、安全审计摘要和本地绝对路径；
- Reset 门禁：缺少 `--confirm-simulated-data` 时命令以非零状态拒绝；自动化隔离数据库测试确认 reset 只处理目标 manifest 的 C-07 非旗舰 ID，并保留四个旗舰身份和无关记录；
- 真实 PostgreSQL seed：首次执行创建 4 个 C-07 岗位、12 个非旗舰用户和 12 个员工画像；第二次执行创建数全部为 0，并验证 36 项既有关系；
- 真实 PostgreSQL reset：确认参数下精确移除 12 个 C-07 非旗舰用户和 4 个 C-07 岗位，并在同一事务重建 12 个用户；随后再次 seed 创建数全部为 0、验证 36 项关系；
- C-06 基础保留：reset 后再次执行 `pnpm identity:bootstrap`，确认 1 个组织、4 个部门、4 个基础岗位、4 个角色、4 个旗舰用户均存在，凭据新增和轮换数均为 0；
- 后端测试：42 项 Pytest 和 151 个契约子测试通过，其中 5 项 C-07 测试覆盖字节级复现、启动隔离、匿名白名单、reset 确认门禁、幂等 seed 和范围化 reset；
- 前端与构建：TypeScript、ESLint、7 个 Vitest 文件共 26 项测试和 Web 生产构建通过；生成的 `packages/mock-data` 保持 Web 与 Android 旗舰流程兼容；
- 契约：`backend/scripts/export_contracts.py --check` 确认 121 个生成产物无漂移；
- 仓库卫生：`git diff --check` 通过；`.env`、standard/stress 生成物、匿名导出和授权二维码均由 Git 忽略，C-07 未执行 `git add`、`commit`、`push` 或创建 PR。

Pytest 当前仍报告一条来自 FastAPI `TestClient` 与 Starlette/httpx 兼容层的上游弃用提示，不是 C-07 代码警告，也不影响测试通过结果。迁移专项命令未列为 C-07 通过证据：当前工作树没有新增或修改数据库迁移，且本机未配置独立的 `MIGRATION_TEST_DATABASE_URL`。

## 数据安全与结论边界

所有组织、人员、岗位、文档、题目、任务、审批、测评和 Agent 数据均为模拟数据。上述验证证明当前生成、校验、身份装载、范围化重置和前端适配代码按已批准规则运行，不证明真实企业制度、培训效率、人员能力或风险水平已经改善，也不得用于绩效、晋升、处罚或其他人事结论。

本地 `.env` 只保存开发环境密码和连接配置，不进入 Git、数据集、manifest、匿名导出、日志或飞书。完整培训、知识、审批、测评和 Agent 业务表尚未建立，因此 C-07 只向现有身份表装载兼容对象，其余对象保留为版本化文件。
