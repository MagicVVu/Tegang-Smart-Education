# 本地开发

## 当前可执行检查

```bash
git status --short
git remote -v
git branch --show-current
```

仓库目前处于规划阶段，尚无已批准的应用依赖或启动命令。后续选定技术方案后，应在此补充：

- 前置软件及版本；
- 安装命令；
- 数据初始化；
- 各服务启动方式；
- 测试与评测命令；
- 常见问题和清理方式。

真实配置应复制 `.env.example` 为本地 `.env` 后填写；不得提交 `.env`。
